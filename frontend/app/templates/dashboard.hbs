{{outlet}}
<h1>Dashboard</h1>

(navigation bar to come)

{{ model }}

{{#each model as |movie|}}
  {{#video-player title=movie.title link=movie.link date=movie.date video-url=movie.videoUrl}}
  {{/video-player}}
{{/each}}
